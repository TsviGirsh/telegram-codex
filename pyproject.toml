[project]
name = "telegram-codex"
version = "0.1.0"
description = "A Telegram bot that runs Codex tasks in a configured Git repository."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv==1.2.2",
    "python-telegram-bot==22.7",
]
